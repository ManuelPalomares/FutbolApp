Ext.define('EscuelaFutbol.model.Implementos', {
    extend: 'Ext.data.Model',

    requires: [
        'Ext.data.Field'
    ],

    fields: [
        {
            name: 'codigo'
        },
        {
            name: 'nombre'
        }
    ]
});